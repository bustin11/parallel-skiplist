
#include "test.h"
#include "skiplist.h"

#include <stdio.h>
#include <assert.h>


int main() {
    
    assert(test1());
    assert(test2());
    assert(test3());
    assert(test4());
    assert(test5());
    assert(test6());
    assert(test7());
    assert(test8());
    assert(test9());
    assert(test10());
    assert(test11());
    assert(test12());

    printf("All Tests Passed!\n");
}